# KALAVAI Interactive Paper

Open `paper/interactive/index.html` in a browser, or serve the repo root locally:

```powershell
python -m http.server 8000
```

Then visit:

```text
http://localhost:8000/paper/interactive/
```

The page is a standalone static site that reuses committed figures in `paper/figures/` and summarizes committed results from `results/`.
